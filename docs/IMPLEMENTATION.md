# Implementation

API requests use:

- `Accept: application/json`
- `Authorization: Bearer <api-key>`
- `User-Agent: tinydialog-cli/<version>`
- `X-Compatibility-Date: 2026-07-28`
- 30-second timeouts

The server currently ignores the compatibility date. It is reserved for future compatibility behavior.
